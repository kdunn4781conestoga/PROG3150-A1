{
  "name": "json-server",
  "version": "1.0.0",
  "description": "For loading locations for app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch database.json -H 127.0.0.1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.2"
  }
}
